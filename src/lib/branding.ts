/**
 * Product naming lives here so it can be changed in one place.
 * The hackathon rules ask that the project be named by a person, not by a
 * model; PRODUCT_NAME is the placeholder to replace.
 */
export const PRODUCT_NAME = "Redaction Gateway";
export const PRODUCT_TAGLINE = "Let an agent read your document without sending it.";
