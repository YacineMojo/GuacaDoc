export const PRODUCT_NAME = "GuacaDoc";
export const PRODUCT_TAGLINE = "Give your AI agent the taste, not the recipe.";

/**
 * The metaphor, stated once so the copy stays consistent everywhere else.
 * Flesh is what an agent receives, the stone is what it never does, and
 * mashing is one-way: you cannot get the avocado back from the guacamole.
 */
export const PRODUCT_SUMMARY =
  "Your document opens in this tab and never leaves it. An agent reads it through tools that pass on the meaning and hold back the names, accounts and figures you mark.";
